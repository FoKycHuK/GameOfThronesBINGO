@using System.ComponentModel.DataAnnotations
@using GoTB.Domain.Entities
@using GoTB.WebUI.Infrastructure
@model GoTB.Domain.Entities.IsAlive

@{
    var a = ((IsAlive[]) Enum.GetValues(typeof (IsAlive))).Select(x => x.GetAttribute<DisplayAttribute>().Name);
}
@Html.DropDownListFor(m => m,
 new SelectList(a,
 Model.ToString()))